# dbt to BigQuery

This branch contains the dbt project made on dbt cloud to push transformations to a dataset in BigQuery called dim_monthly_zone_revenue. It uses the green and yellow taxi tables along with zone information to compute total revenue earned monthly. This is a dev branch exclusively for dbt cloud development.
